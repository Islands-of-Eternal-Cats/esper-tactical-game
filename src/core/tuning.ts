/**
 * Стартовые числа среза.
 *
 * Это не результат расчёта, а разумная точка старта из плана прототипа,
 * чтобы не подбирать с нуля. Проверяются на живом человеке и почти наверняка
 * сдвинутся. Всё, что настраивается, лежит здесь и больше нигде.
 */

/** 20 тиков в секунду. Шаг всегда одинаковый: tick() не принимает время. */
export const TICK_MS = 50

/** Защита от догоняния после свёрнутой вкладки. */
export const MAX_STEPS = 8

// ─── Двор ────────────────────────────────────────────────────────────────

/** Клетка ≈ 1 метр: меньше кота она быть не может, иначе поедет занятость. */
export const GRID_W = 20
export const GRID_H = 20

export const PILES_MIN = 12
export const PILES_MAX = 16

/** Объём кучи в единицах. Куча должна уходить за 5–12 секунд. */
export const PILE_VOL_MIN = 3
export const PILE_VOL_MAX = 8

// ─── Ржавый ──────────────────────────────────────────────────────────────

/** Миллисекунд на клетку по прямой. Диагональ дороже в 1,4 раза. */
export const WALK_MS_PER_CELL = 400

/** Миллисекунд на одну единицу мусора. Единица — ещё и точка решения. */
export const SUCK_MS_PER_UNIT = 1500

/** Ёмкость пылесоса в единицах: опустошение раз в 1–2 минуты работы. */
export const VACUUM_CAPACITY = 14

/** Опустошение у контейнера. */
export const DUMP_MS = 1200

// ─── Ответы на открытые вопросы плана прототипа ──────────────────────────
// Каждое из этих чисел — не техническое решение, а дизайнерское. Они прямо
// названы открытыми в плане прототипа; здесь стоит первая версия ответа,
// которую и проверяет срез.

// Радиус зоны — в shared/protocol.ts: его выбирает главный поток.

/**
 * Зона не вечна: она исчерпывается. Когда мусора в ней не осталось, Ржавый
 * доходит до центра, осматривается и возвращается к своему порядку.
 * Так игрок видит конец приоритета, а не гадает, действует он ещё или нет.
 */
export const SURVEY_MS = 1600

/**
 * Порог «почти доубрал», в единицах объёма.
 * Кот, которому осталось меньше этого, не бросает кучу по клику.
 * Это и есть характер, выраженный правилом, а не текстом.
 */
export const NEARLY_DONE_UNITS = 1.5
